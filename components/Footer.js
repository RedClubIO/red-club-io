import sFooter from '../styles/Footer.module.css'

import UILink from "./UI/UILink";


import { ABOUT, CHARITY, FAQ, PRIVACY, MINT, PROJECTS, REDCLUB, ROADMAP, TEAM, TERMS } from "./data/links";

export default function Footer() {
	return (
		<footer className={sFooter.footer}>
			<div className={sFooter.footer_links}>
                    
			</div>
		</footer>
	)
}
	